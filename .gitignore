# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Game artefacts. A decision log and a save are the same file, and both are
# written by simply playing (or by running the test suite) - they describe one
# session on one machine, not the project.
hts_logs/
hts_saves/
